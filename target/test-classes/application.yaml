spring:
  datasource:
    url: jdbc:tc:postgresql:15-alpine:///
    #url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    username: test
    password: test
  kafka:
    consumer:
      auto-offset-reset: earliest